/*
import org.junit.Assert;
import org.junit.Test;

public class TaskTest {
    Task task = new Task();

    @Test
    public void testSolution() {
        Assert.assertEquals(getFailedMessage("getVariableChar"), task.getVariableChar(), Task.variableChar);
        Assert.assertEquals(getFailedMessage("getVariableByte"), task.getVariableByte(), Task.variableByte);
        Assert.assertEquals(getFailedMessage("getVariableShort"), task.getVariableShort(), Task.variableShort);
        Assert.assertEquals(getFailedMessage("getVariableInt"), task.getVariableInt(), Task.variableInt);
        Assert.assertEquals(getFailedMessage("getVariableLong"), task.getVariableLong(), Task.variableLong);
        Assert.assertEquals(getFailedMessage("getVariableFloat"), task.getVariableFloat(), Task.variableFloat, 0);
        Assert.assertEquals(getFailedMessage("getVariableDouble"), task.getVariableDouble(), Task.variableDouble, 0);
        Assert.assertEquals(getFailedMessage("getVariableBoolean"), task.getVariableBoolean(), Task.variableBoolean);
    }

    private String getFailedMessage(String methodName) {
      return String.format("Некорректное значение в ответе метода %s", methodName);
    }
}*/
